extends template 

block content 
    table 
        thead 
            tr 
                th(scope="col") Email
                th(scope="col") Role
        tbody 
            each user in users
                tr 
                    td= user.email
                    td= user.role
